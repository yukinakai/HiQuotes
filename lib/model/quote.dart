class Quote {
  Quote(
      {required this.id,
       required this.title,
       required this.url,
       required this.content,
       required this.updatedAt});

  final String id, title, url, content, updatedAt;
}
